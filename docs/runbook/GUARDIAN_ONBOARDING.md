# Guardian Onboarding Verification

Run `npm run phase35:guardian-verify` in local development.

The verifier proves real API behavior for guardian account creation, duplicate email, student creation, PENDING relationship, approval, guardian read access, revoke, object-level denial, re-request, re-approval, and development fixture deactivation. It does not write directly to databases.
