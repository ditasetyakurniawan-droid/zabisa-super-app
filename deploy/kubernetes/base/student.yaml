apiVersion: apps/v1
kind: Deployment
metadata:
  name: student
  namespace: zabisa-dt
spec:
  replicas: 2
  selector:
    matchLabels: {app: student}
  template:
    metadata:
      labels: {app: student, tier: backend}
    spec:
      serviceAccountName: zabisa-app
      securityContext:
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: student
          image: harbor-dt.co.id/zabisa/student:REPLACE_SHA
          ports: [{containerPort: 8083}]
          env:
            - {name: ENVIRONMENT, value: dt}
            - {name: MYSQL_HOST, value: db-dt}
          envFrom:
            - secretRef: {name: student-runtime}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities: {drop: ["ALL"]}
          resources:
            requests: {cpu: 50m, memory: 64Mi}
            limits: {cpu: 500m, memory: 256Mi}
          livenessProbe:
            httpGet: {path: /health/live, port: 8083}
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet: {path: /health/ready, port: 8083}
            initialDelaySeconds: 5
            periodSeconds: 10
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector: {matchLabels: {app: student}}
---
apiVersion: v1
kind: Service
metadata:
  name: student
  namespace: zabisa-dt
spec:
  selector: {app: student}
  ports: [{port: 8083, targetPort: 8083}]
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: student
  namespace: zabisa-dt
spec:
  minAvailable: 1
  selector: {matchLabels: {app: student}}
