apiVersion: apps/v1
kind: Deployment
metadata:
  name: content
  namespace: zabisa-dt
spec:
  replicas: 2
  selector:
    matchLabels: {app: content}
  template:
    metadata:
      labels: {app: content, tier: backend}
    spec:
      serviceAccountName: zabisa-app
      securityContext:
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: content
          image: harbor-dt.co.id/zabisa/content:REPLACE_SHA
          ports: [{containerPort: 8082}]
          env:
            - {name: ENVIRONMENT, value: dt}
            - {name: MYSQL_HOST, value: db-dt}
          envFrom:
            - secretRef: {name: content-runtime}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities: {drop: ["ALL"]}
          resources:
            requests: {cpu: 50m, memory: 64Mi}
            limits: {cpu: 500m, memory: 256Mi}
          livenessProbe:
            httpGet: {path: /health/live, port: 8082}
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet: {path: /health/ready, port: 8082}
            initialDelaySeconds: 5
            periodSeconds: 10
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector: {matchLabels: {app: content}}
---
apiVersion: v1
kind: Service
metadata:
  name: content
  namespace: zabisa-dt
spec:
  selector: {app: content}
  ports: [{port: 8082, targetPort: 8082}]
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: content
  namespace: zabisa-dt
spec:
  minAvailable: 1
  selector: {matchLabels: {app: content}}
