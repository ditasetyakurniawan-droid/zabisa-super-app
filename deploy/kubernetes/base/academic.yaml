apiVersion: apps/v1
kind: Deployment
metadata:
  name: academic
  namespace: zabisa-dt
spec:
  replicas: 2
  selector:
    matchLabels: {app: academic}
  template:
    metadata:
      labels: {app: academic, tier: backend}
    spec:
      serviceAccountName: zabisa-app
      securityContext:
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: academic
          image: harbor-dt.co.id/zabisa/academic:REPLACE_SHA
          ports: [{containerPort: 8085}]
          env:
            - {name: ENVIRONMENT, value: dt}
            - {name: MYSQL_HOST, value: db-dt}
          envFrom:
            - secretRef: {name: academic-runtime}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities: {drop: ["ALL"]}
          resources:
            requests: {cpu: 50m, memory: 64Mi}
            limits: {cpu: 500m, memory: 256Mi}
          livenessProbe:
            httpGet: {path: /health/live, port: 8085}
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet: {path: /health/ready, port: 8085}
            initialDelaySeconds: 5
            periodSeconds: 10
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector: {matchLabels: {app: academic}}
---
apiVersion: v1
kind: Service
metadata:
  name: academic
  namespace: zabisa-dt
spec:
  selector: {app: academic}
  ports: [{port: 8085, targetPort: 8085}]
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: academic
  namespace: zabisa-dt
spec:
  minAvailable: 1
  selector: {matchLabels: {app: academic}}
