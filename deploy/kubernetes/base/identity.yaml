apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity
  namespace: zabisa-dt
spec:
  replicas: 2
  selector:
    matchLabels: {app: identity}
  template:
    metadata:
      labels: {app: identity, tier: backend}
    spec:
      serviceAccountName: zabisa-app
      securityContext:
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: identity
          image: harbor-dt.co.id/zabisa/identity:REPLACE_SHA
          ports: [{containerPort: 8081}]
          env:
            - {name: ENVIRONMENT, value: dt}
            - {name: MYSQL_HOST, value: db-dt}
          envFrom:
            - secretRef: {name: identity-runtime}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities: {drop: ["ALL"]}
          resources:
            requests: {cpu: 50m, memory: 64Mi}
            limits: {cpu: 500m, memory: 256Mi}
          livenessProbe:
            httpGet: {path: /health/live, port: 8081}
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet: {path: /health/ready, port: 8081}
            initialDelaySeconds: 5
            periodSeconds: 10
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector: {matchLabels: {app: identity}}
---
apiVersion: v1
kind: Service
metadata:
  name: identity
  namespace: zabisa-dt
spec:
  selector: {app: identity}
  ports: [{port: 8081, targetPort: 8081}]
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: identity
  namespace: zabisa-dt
spec:
  minAvailable: 1
  selector: {matchLabels: {app: identity}}
